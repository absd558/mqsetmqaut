---
# tasks to grant svc-snowmqm user access to MQ
- name: Display queue manager on ZAURNBRAPP002F
  command: '{{ MQ.command_path }}/dspmq'
  become: yes
  become_user: '{{ MQ.user }}'
  when: ansible_hostname == "ZAURNBRAPP002F"
  
  #MQMBPROD01
- name: Backup MQMBSIT01 on ZAURNBRAPP002F
  command: '{{ MQ.command_path }}/dmpmqcfg -m MQMBSIT01 -a -t all -x all > /tmp/MQMBSIT01.mqsc'
  become: yes
  become_user: '{{ MQ.user }}'
  when: ansible_hostname == "ZAURNBRAPP002F"
